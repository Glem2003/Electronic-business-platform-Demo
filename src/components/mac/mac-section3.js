import SectionTitle from "../common/section-title";
import MacInfoBox from "./mac-info-box";

const MacThirdSection = () => {
    return (
        <div className="section section3">
            <SectionTitle title="看 Mac 在企業顯身手。" />
            <MacInfoBox/>
        </div>
    )
}

export default MacThirdSection;