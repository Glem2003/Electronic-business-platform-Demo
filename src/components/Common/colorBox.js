import '../../Sass/component/colorBox.sass';

const ColorBox = ({ color }) => {
    return (
        <div
            id={color}
            className="box"
        >
        </div>
    )
}

export default ColorBox;