const ContantTitle = ({ title, subtitle }) => {
    return (
        <div className="title">
            <p>{title}</p>
            <span>{subtitle}</span>
        </div>
    )
}

export default ContantTitle;